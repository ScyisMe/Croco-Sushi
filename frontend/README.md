# Croco Sushi Frontend

Frontend додаток для сайту доставки суші Croco Sushi, побудований на Next.js 14+ з App Router.

## Технологічний стек

- **Next.js 14+** - React фреймворк з App Router
- **TypeScript** - типізація
- **Tailwind CSS** - стилізація
- **React Query** - управління станом сервера
- **Zustand** - управління клієнтським станом
- **React Hook Form + Zod** - форми та валідація
- **Axios** - HTTP клієнт

## Швидкий старт

### 1. Встановлення залежностей

```bash
cd frontend
npm install
# або
yarn install
# або
pnpm install
```

### 2. Налаштування змінних середовища

Створіть файл `.env.local` в папці `frontend`:

```bash
# Windows PowerShell
New-Item -Path .env.local -ItemType File

# Або просто створіть файл вручну
```

Додайте в `.env.local`:
```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

**Важливо:** Переконайтеся, що backend запущений на `http://localhost:8000`

### 3. Запуск проекту

```bash
npm run dev
# або
yarn dev
# або
pnpm dev
```

Відкрийте [http://localhost:3000](http://localhost:3000) у браузері.

### 4. Збірка для production

```bash
npm run build
npm start
```

## Структура проекту

```
frontend/
├── app/                    # Next.js App Router
│   ├── admin/             # Адмін-панель
│   ├── checkout/          # Оформлення замовлення
│   ├── menu/              # Каталог товарів
│   ├── products/          # Сторінки товарів
│   ├── profile/           # Особистий кабінет
│   ├── layout.tsx         # Корінь layout
│   └── page.tsx           # Головна сторінка
├── components/            # React компоненти
│   ├── Cart.tsx           # Кошик
│   ├── Header.tsx         # Шапка сайту
│   ├── Footer.tsx         # Підвал
│   └── ...
├── lib/                   # Утиліти та конфігурація
│   ├── api/              # API клієнт
│   └── types/            # TypeScript типи
├── store/                # Zustand stores
│   └── cartStore.ts      # Store для кошика
└── public/               # Статичні файли
```

## Основні функції

- 🏠 Головна сторінка з каталогом
- 🛒 Кошик та оформлення замовлень
- 👤 Особистий кабінет
- 🔐 Аутентифікація та реєстрація
- 📱 Адаптивний дизайн
- 💳 Інтеграція з платіжною системою (LiqPay)
- 🛡️ Адмін-панель

## Розробка

### Створення нового компонента

Використовуйте структуру папок у `components/`

### API інтеграція

API клієнт знаходиться в `lib/api/client.ts`. Використовуйте React Query для запитів до API.

### Стилізація

Проект використовує Tailwind CSS. Додавайте кастомні класи в `tailwind.config.ts`.

## Troubleshooting

### Помилка підключення до API

1. Перевірте, що backend запущений на `http://localhost:8000`
2. Перевірте файл `.env.local` та змінну `NEXT_PUBLIC_API_URL`
3. Перевірте CORS налаштування в backend

### Помилки TypeScript

```bash
npm run lint
```

### Очищення кешу

```bash
rm -rf .next
npm run dev
```

## Більше інформації

Див. [Next.js документацію](https://nextjs.org/docs) для деталей про Next.js.
